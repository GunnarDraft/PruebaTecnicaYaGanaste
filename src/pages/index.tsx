import { useEffect } from 'react';
import BankList from '../components/BankList';

const Home = () => {
    return (
        <div> 
             <BankList />
        </div>
    );
};

export default Home;